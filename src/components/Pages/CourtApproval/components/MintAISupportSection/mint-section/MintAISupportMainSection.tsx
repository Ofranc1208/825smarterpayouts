import MintBackground from './MintBackground';
import MintContent from './MintContent';

export default function MintAISupportMainSection() {
  return (
    <MintBackground>
      <MintContent />
    </MintBackground>
  );
}
